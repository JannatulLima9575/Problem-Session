// # Find the Perimeter of a Rectangle
// Problem-5: Create a function that takes length and width and finds the perimeter of a rectangle.
function findPerimeter(length, width) {
	let checkRectangle = (length + width) * 2;
	return checkRectangle;
}