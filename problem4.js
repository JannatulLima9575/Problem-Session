// # Convert Minutes into Seconds
// Problem-4 Write a function that takes an integer minutes and converts it to seconds.
function convert(minutes) {
	let minutesToSeconds = minutes * 60;
    return minutesToSeconds;
}