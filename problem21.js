// # Fix the Expression
// Problem-21: Fix the code in the Code tab so the function returns true if and only if x is equal to 7.
function isSeven(x) {
	if( x === 7) {
        return true;
    }
    else {
        return false
    }
}